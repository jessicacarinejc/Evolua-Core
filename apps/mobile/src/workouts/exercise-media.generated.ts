// Gerado/atualizado por scripts/prepare-mobile-exercise-media.mjs durante o build Android.
// O arquivo commitado permanece vazio para permitir typecheck sem baixar mídia.
export type GeneratedLocalExerciseMedia = {
  source: number;
  finalQuality: 'realistic-human-demo' | 'animated-fallback';
  license?: string;
  attribution?: string;
};

export const generatedLocalExerciseMedia: Record<string, GeneratedLocalExerciseMedia> = {};
